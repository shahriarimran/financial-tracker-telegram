# Legacy Python bot

The former Python polling implementation is retained for historical reference only. It is unsupported and excluded from the production runtime.

Production is Windows Task Scheduler → `scripts/RunMacro.vbs` → Excel/VBA → Telegram.
